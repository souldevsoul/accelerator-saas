{
  "name": "db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "push": "prisma db push",
    "seed": "tsx seed.ts",
    "studio": "prisma studio",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "tsx": "^4.7.0",
    "@types/node": "^20"
  }
}
